<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sign Up</title>

    <style>
        * {
            box-sizing: border-box;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }

        body {
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #1e1e2f, #2b2b45);
            display: flex;
            justify-content: center;
            align-items: center;
            color: #f5f5f5;
        }

        .card {
            background: #111827;
            width: 420px;
            padding: 30px;
            border-radius: 14px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        h1 {
            text-align: center;
            margin-top: 0;
            font-size: 26px;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-top: 20px;
        }

        label {
            font-size: 14px;
            color: #d1d5db;
        }

        input {
            padding: 12px;
            border-radius: 8px;
            border: none;
            outline: none;
            background: #1f2933;
            color: #f5f5f5;
            font-size: 15px;
        }

        input::placeholder {
            color: #9ca3af;
        }

        input[type="submit"] {
            margin-top: 10px;
            background: #2563eb;
            cursor: pointer;
            font-weight: 600;
            transition: 0.2s ease;
        }

        input[type="submit"]:hover {
            background: #1d4ed8;
        }

        .messages {
            margin-top: 15px;
        }

        .error {
            background: rgba(220, 38, 38, 0.15);
            border-left: 4px solid #dc2626;
            padding: 8px;
            margin-bottom: 8px;
            border-radius: 6px;
            color: #fecaca;
        }

        .success {
            background: rgba(34, 197, 94, 0.15);
            border-left: 4px solid #22c55e;
            padding: 8px;
            margin-bottom: 8px;
            border-radius: 6px;
            color: #bbf7d0;
        }

        .back {
            margin-top: 20px;
            text-align: center;
        }

        .back a {
            color: #93c5fd;
            text-decoration: none;
            font-size: 14px;
        }

        .back a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="card">
        <h1>Create an Account</h1>

        <div class="messages">
            <% if (error) { %>
                <% error.forEach(msg => { %>
                    <div class="error"><%= msg %></div>
                <% }) %>
            <% } %>

            <% if (success) { %>
                <% success.forEach(msg => { %>
                    <div class="success"><%= msg %></div>
                <% }) %>
            <% } %>
        </div>

        <form method="POST" action="/signup">
            <div>
                <label for="u">Username</label>
                <input name="username" id="u" placeholder="Choose a username" required>
            </div>
            <input type="hidden" name="csrf" value = "<%= csrf %>"></input>
            <div>
                <label for="p">Password</label>
                <input type="password" name="password" id="p" placeholder="Create a password" required>
            </div>

            <input type="submit" value="Sign Up">
        </form>

        <div class="back">
            <a href="/">← Back to Home</a>
        </div>
    </div>
</body>
</html>
