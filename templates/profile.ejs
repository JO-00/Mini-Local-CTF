<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Profile</title>

    <style>
        * {
            box-sizing: border-box;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }

        body {
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #1e1e2f, #2b2b45);
            display: flex;
            justify-content: center;
            align-items: center;
            color: #f5f5f5;
        }

        .card {
            background: #111827;
            width: 450px;
            padding: 30px;
            border-radius: 14px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        h1 {
            text-align: center;
            margin-top: 0;
            font-size: 26px;
        }

        .info {
            margin-top: 20px;
            padding: 15px;
            background: #1f2933;
            border-radius: 10px;
        }

        .info p {
            margin: 8px 0;
            color: #d1d5db;
        }

        .info span {
            color: #f5f5f5;
            font-weight: 600;
        }

        .actions {
            margin-top: 25px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        a {
            text-decoration: none;
            text-align: center;
            padding: 10px;
            border-radius: 8px;
            background: #2563eb;
            color: white;
            transition: 0.2s ease;
        }

        a:hover {
            background: #1d4ed8;
        }

        .secondary {
            background: #374151;
        }

        .secondary:hover {
            background: #4b5563;
        }

        .danger {
            background: #dc2626;
        }

        .danger:hover {
            background: #b91c1c;
        }
    </style>
</head>

<body>
    <div class="card">
        <h1>Your Profile</h1>

        <div class="info">
            <p>Username: <span><%= user %></span></p>
            <p>Status: <span>Authenticated</span></p>
            <p>Session: <span>Active</span></p>
        </div>

        <div class="actions">
            <a href="/">Home</a>
            <a href="/logout" class="secondary">Log Out</a>
            <a href="/delete-account" class="danger">Delete Account</a>
        </div>
    </div>
</body>
</html>
